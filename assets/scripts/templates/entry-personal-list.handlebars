{{#each entries as |entry|}}
  {{log book}}

  <section data-id="{{entry.id}}" class='entry-log'>
    <h3>Title:</h3>
    <h6>
      {{entry.name}}
    </h6>
    <p>
      {{entry.content}}
    </p>
    <button>Delete</button>
    <button>Edit</button>
  </section>

{{/each}}
